apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: poc-tradingagents-studio
  labels:
    app: web
    autopoc.io/project: tradingagents-studio
spec:
  type: ClusterIP
  selector:
    app: web
  ports:
    - port: 8080
      targetPort: 8080
